#!/bin/bash

# Pre-push hook
# Version bumping is now done manually by the developer before creating a PR.
# CI will verify the version was incremented.

# This hook is intentionally empty - version management is developer-driven.
# See docs/devops/versioning.md for the new workflow.

exit 0
